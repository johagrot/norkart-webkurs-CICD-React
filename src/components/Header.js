import React from "react";

const styles = {
  height: "60px",
  width: "100vw",
  padding: "10px",
  textAlign: "center",
  fontSize: "60px",
};

const Header = () => {
  return (
    <div style={styles}>
      <header>KART ????</header>
    </div>
  );
};

export default Header;
